{
  "name": "trustmesh-chaincode",
  "version": "1.0.0",
  "description": "TrustMesh chaincode — identity, RBAC and digital-asset registries with application-level multi-party governance",
  "main": "dist/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "start": "fabric-chaincode-node start",
    "test": "node --test dist-test/*.test.js",
    "pretest": "tsc -p tsconfig.test.json"
  },
  "engineStrict": true,
  "license": "MIT",
  "dependencies": {
    "fabric-contract-api": "^2.5.4",
    "fabric-shim": "^2.5.4"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "typescript": "^5.4.5"
  }
}
